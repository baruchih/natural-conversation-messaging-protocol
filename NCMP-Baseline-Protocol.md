# NCMP Baseline Protocol

**Status:** Retired. Not a specification.  
**Date:** September 2026

This file was a second full machine for an earlier lexical-control draft. It is not aligned with current `process` and MUST NOT be implemented.

The only normative protocol text is [`ncmp/NCMP.md`](ncmp/NCMP.md). The reference decoder is [`ncmp/reference/ncmp.ts`](ncmp/reference/ncmp.ts). Conformance is `npm test`.

Historical Profile 0 argument-transport evidence remains in `v4/baseline.ts`. That machine is evidence, not v0.1 control.
